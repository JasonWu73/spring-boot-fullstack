/**
 * 1. 登录次数最多的前三个用户，饼图（占总登录数），点击饼图的某一块，跳转到用户详情页
 * 2. SSE 实时显示当前在线用户，滚动列表
 * 3. 最近七天的登录次数，折线图
 */
export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  )
}
